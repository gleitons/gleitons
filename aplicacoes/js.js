function colapse() {
    const acc = document.getElementById('questionsAccordeon');

    acc2 = acc('click',)

    if (acc==1) {
        acc.classList.remove('oculto')
      } else {
        acc.classList.add('oculto')
      }
}

